# Deploy the existing Elephant Pants Site

Sync the existing Elephant Pants Site from the latest validated `main` commit of `valerol/ep_dashboard`, save a version with that exact commit SHA, verify the production build, and deploy it to the existing production Site at `https://elephant-pants.xolepa83.chatgpt.site/`. Use the existing `project_id` from `.openai/hosting.json`. Do not create a new Site. Confirm that the deployed version reports the same commit SHA.
